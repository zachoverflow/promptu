using System;
using System.Collections.Generic;
using System.Text;
using System.IO;

namespace ZachJohnson.Promptu.FileFileSystem
{
    internal class CustomStreamReader : StreamReader
    {
        private string peekedLine;

        public CustomStreamReader(Stream stream)
            : base(stream)
        {
        }

        public string PeekLine()
        {
            this.peekedLine = this.ReadLine();
            return this.peekedLine;
        }

        public override string ReadLine()
        {
            if (this.peekedLine != null)
            {
                string line = this.peekedLine;
                this.peekedLine = null;
                return line;
            }

            return base.ReadLine();
            //base.re
        }

        //public byte[] ReadUntil(byte[] stopReading)
        //{
        //    byte[] lastRead = new byte[stopReading.Length];
        //    using (MemoryStream stream = new MemoryStream())
        //    {
        //        byte[] buffer = new byte[4096];

        //        int bytesRead = 0;
        //        do
        //        {
        //            bytesRead = this.BaseStream.Read(buffer, 0, buffer.Length);
        //            for (int i = 0; i < bytesRead; i++)
        //            {
        //                if (i < stopReading.Length)
        //                {
        //                    lastRead[i] = buffer[i];
        //                }
        //                else
        //                {
                            
        //                }
        //            }
        //        }
        //        while (bytesRead > 0);
        //    }
        //}
    }
}
